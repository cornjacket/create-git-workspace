# tasks/

Task tracker for building the `create-git-workspace` generator. One file per
task, `NNN-slug.md`, newest ideas appended. The live plan is `../PLAN.md`; these
are its executable breakdown.

Status legend: `todo` · `in-progress` · `blocked` · `done`.
